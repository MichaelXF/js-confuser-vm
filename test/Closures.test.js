// Closures:

// Variants to create
// Basic closure with a single variable
// Closure with multiple variables
// Closure that captures variables from an outer function
// Closure that modifies captured variables
// Nested closures (closure inside another closure)

// Also:
// exposed 'closures' out of the VM - then called from test
